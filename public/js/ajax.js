// Формирование отчета
const formreport = document.getElementById("get-report");
formreport.addEventListener("submit", async(e) => {
    e.preventDefault();

    //const response = fetch("/get-report", {
    //    method: "POST",
    //    headers: {
    //        "Content-Type": "application/json"
    //    },
    //    body: JSON.stringify({})
    //})
});